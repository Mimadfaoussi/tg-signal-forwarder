[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
asyncio_mode = "strict"
testpaths = ["tests"]

[[tool.mypy.overrides]]
module = "telethon.*"
ignore_missing_imports = true
